# listing / deleting generations

## listing
```bash
sudo nix-env --list-generations --profile /nix/var/nix/profiles/system
```

## deleting
```bash
sudo nix-env --delet-generations --profile /nix/var/nix/profiles/system *filter options*
```

# done
